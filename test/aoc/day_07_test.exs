defmodule AoC.Day07Test do
  use ExUnit.Case

  alias AoC.Day07

  test "gets the right answer for part one" do
    assert Day07.part_one() == 0
  end

  test "gets the right answer for part two" do
    assert Day07.part_two() == 0
  end
end
